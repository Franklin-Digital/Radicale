"""Franklin customizations for Radicale (DAYTRADE-778).

Kept OUT of the upstream `radicale/` tree so `git merge upstream/master` stays
a fast-forward. Referenced from config as `franklin_radicale.auth_userdb`.
"""
